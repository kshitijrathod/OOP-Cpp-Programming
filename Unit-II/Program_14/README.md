# Program 14

**Unit:** II - Inheritance

## Brief Description
Creates and uses a nested class inside another class.

## Source
Based on the supplied Unit II practical code book.

## Compile
```bash
g++ -std=c++17 program14.cpp -o program.exe
program.exe
```

## Output
See `output.txt` for the locally verified run output.
